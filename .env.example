# Local development only. Replace these values if the stack is reachable beyond your machine.
POSTGRES_PASSWORD=taskio_local_only
MIGRATOR_PASSWORD=taskio_migrator_local_only
APP_DB_PASSWORD=taskio_app_local_only
APP_ORIGIN=http://localhost:9090
